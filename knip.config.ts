import type {KnipConfig} from 'knip';

const config: KnipConfig = {
    ignore: [],
    ignoreDependencies: [
        'tailwindcss',
        'clsx',
        'lucide-react',
        'tw-animate-css',
    ],
};

export default config;
